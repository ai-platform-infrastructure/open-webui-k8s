apiVersion: v1
kind: ConfigMap
metadata:
  name: ingestion-service-env
  namespace: {{ .Release.Namespace }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
