apiVersion: v1
kind: ConfigMap
metadata:
  name: retrieval-agent-env
  namespace: {{ .Release.Namespace }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
